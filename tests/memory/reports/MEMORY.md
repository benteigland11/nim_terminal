# nim_terminal — Memory Report

This file is the rolling artifact of the memory-test suite. It is the
public receipt that nim_terminal takes memory discipline seriously.
Numbers below are regenerated by CI; the structure is stable.

## Day 1 baseline — 2026-04-26

**Host**
- OS: Linux fedora 6.19.9-200.fc43.x86_64 (Fedora 43)
- Nim: 2.2.8 (compiled 2026-02-23)
- Build: `nim c -d:release src/nim_terminal.nim`
- Binary SHA-256: `d97a4f2800c6feb3d6080e118fb843615aa911b8975aa9fec56326c802e8091a`
- Runtime: software OpenGL (LIBGL_ALWAYS_SOFTWARE=1) under Xvfb 1280x800x24

**Tier 1 — idle baseline (10s observation)**

```
Run 1: samples=10  min=127432KB  max=127432KB  Δ=0KB    slope=0.00 KB/min
Run 2: samples=10  min=127376KB  max=127376KB  Δ=0KB    slope=0.00 KB/min
Run-to-run variance: 56 KB
```

```







*     *      *     *      *     *      *     *      *      *
min=127376KB max=127376KB Δ=0KB samples=10
```

Verdict: **flat**. Idle render loop allocates nothing measurable.

**Tier 2 — soak scenarios (validated, not yet run)**

| Scenario | What it stresses | Status |
|---|---|---|
| `urandom_flood` | High-throughput VT byte stream | harness ready, soak deferred to nightly CI |
| `scroll_churn` | Scrollback growth + damage tracking | harness ready, soak deferred to nightly CI |
| `sgr_storm` | SGR (color/attr) attribute storage | harness ready, soak deferred to nightly CI |
| `alt_buffer_toggle` | Alt screen save/restore (vim/htop pattern) | harness ready, soak deferred to nightly CI |
| `utf8_mix` | UTF-8 decoder + glyph atlas (CJK/emoji/combining) | harness ready, soak deferred to nightly CI |

Each scenario script proven to emit ≥1KB of output under `timeout 1`.
Slope target: < 100 KB/min over a 5-minute soak.

**Tier 3 — Valgrind**

Deferred to Day 2.

**Tier 4 — GPU memory tracking**

Deferred to Day 2 (blocked on `gpu_renderer.nim` instrumentation).

## Day 2 baseline — 2026-04-26

**Host changes since Day 1**
- Nim: **2.2.8 → 2.2.10** (compiled 2026-04-24)
- New widgets pulled in: `data_terminal_render_attrs_nim`,
  `data_vt_diagnostics_nim`, `universal_resource_budget_nim`
- Release binary SHA-256: `f08076461f00a27db01a6e4c50fce04ca025d51c6501f33f1882175b5a10fb7c`
- Debug binary (valgrind) SHA-256: `3f94a7ebec56233ee606da43f2ff3959b93a6c1726606259bbe0b482b090f032`
- Build (debug for valgrind): `nim c --mm:orc -d:useMalloc -d:debug --debugger:native …`

**Tier 1 — idle baseline (re-run under new toolchain)**

```
Run:  samples=10  min=175516KB  max=175516KB  Δ=0KB  slope=0.00 KB/min
```

Curve still flat. Idle render loop is leak-free.

**Δ from Day 1: +48,084 KB startup RSS.** Curve health unchanged. The jump
is concentrated in startup footprint, not in any per-tick allocation. Likely
attribution: three new widgets statically linked in plus Nim 2.2.10 stdlib
size. Tracked as a follow-up; not a leak.

**Tier 3 — Valgrind (alt_buffer_toggle, 8s)**

```
definitely lost: 0 bytes in 0 blocks
indirectly lost: 0 bytes in 0 blocks
possibly lost:   0 bytes in 0 blocks
still reachable: 20760 bytes in 24 blocks
suppressed:      75751 bytes in 86 blocks
errors:          0
```

Wall time: ~11.5s (8s scenario + valgrind overhead). Suppressions filter
known third-party noise from Mesa/GLFW/fontconfig/X11/ld; everything else
came back clean. **Zero bytes attributed to our code.**

Suppressions file: `tests/memory/valgrind/suppressions.supp`
Runner: `tests/memory/valgrind/run_valgrind.sh`
Parser: `tests/memory/valgrind/parse_leaks.nim` (4/4 unit tests pass)

**Tier 4 — GPU memory tracking**

Still deferred. `src/gpu_renderer.nim` remains in concurrent agent's hot
zone; instrumenting now would clash.

## How this file gets regenerated

CI runs `tests/memory/test_idle_baseline.nim`, the soak harness, and
(weekly) the Valgrind pass. The reports/ directory is rewritten on
success. Past baselines are preserved in git history.
