## Example usage of OS Launcher.

import os_launcher_lib

# We just echo what we would do to avoid opening a browser in automated setups.
echo "To open a URL, call: launchUri(\"https://github.com\")"
